import 'package:flutter/material.dart';

class AddToWhitelist extends StatelessWidget {
  const AddToWhitelist({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
